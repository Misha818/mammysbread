Product
  
    Edit Thumbnail image repair!
    Publish/Unpublish product
    create function that removes unused images
    Description
    meta tags      
    Log in/out for stuff
    - queriov hanum es konkret useri rolin patkanogh functionner@ u dnum es ekranin
    - functionneri mej avelacnum es functionner voronq veraberum en erkrordakan lezunerin. (Thumbnail, Article, Article Category)
    Login form
    Stugel log in eghac a te che, nor menak toghnel ogtvi functionneric
    Add rol
        Rol (name)
        Actions (multyselectial dropdown)
    Edit Rol
    Admin panel
        Add teammate
            Firstname
            Lastname            
            Email
            Rol
            send data to teammate's email for confirmation, where he/she creates her unique username and password
                Min 8 charaqteres
                Max 64 chars
                Nums, lower/upper cases, symbols
                Stugel vor himar passwordner chlini (sagh zroner, tver/tarer achman/nvazman kergerov ...)
                Two factor autentication (send email)
                Disable teammate (temporarily, forever)
CREATE A WRAPPER FOR ACTION PERMISSIONS



    csrf_token
        <meta name="csrf-token" content="{{ csrf_token() }}">

        // Function to get the CSRF token from the meta tag
        function getCsrfToken() {
            return document.querySelector('meta[name="csrf-token"]').getAttribute('content');
        }
        
        const csrfToken = getCsrfToken();


        xhr.setRequestHeader('X-CSRFToken', csrfToken);

===========================================================

    edit-teammate
    
    Contacts {style, backend}

    Header
    Footer
    About {style, content}
    Տեխնիկական բնութագիր
    mobile friendly dashboard header
    https 

- Add user function for superuser (serveri vren es testavorelu, qani vor mutqagrelu es rol, email, ayd mailin gnum a jamanakavor hghum vorov nor employeen mutq a gorcum)

Style ideas
https://copyblogger.com/