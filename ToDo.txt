Product
  
    Edit Thumbnail image repair!
    Publish/Unpublish product
    create function that removes unused images
    Description
    meta tags      
    Log in/out for stuff
    - queriov hanum es konkret useri rolin patkanogh functionner@ u dnum es ekranin
    - functionneri mej avelacnum es functionner voronq veraberum en erkrordakan lezunerin. (Thumbnail, Article, Article Category)
    Login form
    Stugel log in eghac a te che, nor menak toghnel ogtvi functionneric
    Add rol
        Rol (name)
        Actions (multyselectial dropdown)
    Edit Rol
    Admin panel
        Add teammate
            Firstname
            Lastname            
            Email
            Rol
            send data to teammate's email for confirmation, where he/she creates her unique username and password
                Min 8 charaqteres
                Max 64 chars
                Nums, lower/upper cases, symbols
                Stugel vor himar passwordner chlini (sagh zroner, tver/tarer achman/nvazman kergerov ...)
                Two factor autentication (send email)
                Disable teammate (temporarily, forever)
CREATE A WRAPPER FOR ACTION PERMISSIONS



    csrf_token
        <meta name="csrf-token" content="{{ csrf_token() }}">

        // Function to get the CSRF token from the meta tag
        function getCsrfToken() {
            return document.querySelector('meta[name="csrf-token"]').getAttribute('content');
        }
        
        const csrfToken = getCsrfToken();


        xhr.setRequestHeader('X-CSRFToken', csrfToken);


        newCSRFtoken = generate_csrf()
        'newCSRFtoken': newCSRFtoken

        FOR JS 
        let csrfToken = "{{ csrf_token() }}"

    https 
    edit-teammate
    Header
    mobile friendly dashboard header
    Filter untranslated product categories and offer to translate, you should do it in
    
     /product/<prid> where product is tried to be added in another language 

            get pc from def language
            get pc from current lang if current lang is not def lang
            if len(currentLang) < len(defLang)
                add untranslated values to an arr
                z = x ^ y   This should be done with ref keys that are common, ids are not common, they are unique

===========================================================

    product Slider
        AltText does not work
        Cropping on upload does not work properly
        Thumbnail imgs style if less then 5

    Write table creaing function
    BUGS
    ete nkar chka teammatum cropi het miasin done - n el a cuyc talis
    Password, email change

    Configure GIT
    SYNC with GIT
    Docker
    Add/Edit Sliders for product
    Add product types

    Crate header for stuff with | Product | Blog sections. Each section opens releated functions on click
    Contacts { style, backend }

    Footer
    About { style, content }

    Տեխնիկական բնութագիր

    block after 3 - 5 unsuccesfull login tries

    Deside what to do whith articles category of which is disabled

    Quill.JS issues
        More then one spaces disappear on edit mode
        </> sign does not work on view mode but shows when turn to view mode 
        Same issue is with ""

- Add user function for superuser (serveri vren es testavorelu, qani vor mutqagrelu es rol, email, ayd mailin gnum a jamanakavor hghum vorov nor employeen mutq a gorcum)

Style ideas
https://copyblogger.com/

https://fontawesome.com/v4/icons/

    team            fas fa-users
    add teammate    fas fa-user-plus


-- AI tool for job searching
https://github.com/feder-cr/Auto_Jobs_Applier_AIHawk































































































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